import { component$ } from '@builder.io/qwik';

export interface ProjectTileProps {}

export const ProjectTile = component$<ProjectTileProps>(() => {
    return <div>ProjectTile component works!</div>;
});
